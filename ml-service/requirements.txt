# ── Pinned dependencies — DO NOT remove version pins ────────────────────────
# To update: test locally, then update the version number here explicitly.
# Run `pip freeze` after installing to get exact versions.

flask==3.1.2
flask-cors==6.0.2
google-genai==1.61.0
boto3==1.43.8
python-dotenv==1.2.1
gunicorn==22.0.0
pytest==8.1.1
